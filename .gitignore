# === Bí mật — TUYỆT ĐỐI KHÔNG COMMIT ===
.env
.env.local
*.env
!.env.example
secrets/
master.key
*.keystore
deployer-private-key*

# === Java / Maven ===
target/
!.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
!**/maven-wrapper.jar
*.log
hs_err_pid*

# === Node ===
node_modules/
dist/
dist-ssr/
.vite/
*.tsbuildinfo
npm-debug.log*
pnpm-debug.log*

# === Hardhat ===
contracts/artifacts/
contracts/cache/
# typechain: Hardhat 3 sinh ra types/, Hardhat 2 sinh ra typechain-types/
contracts/types/
contracts/typechain-types/
contracts/coverage/
coverage.json
# ABI + bin tách ra cho web3j codegen — dẫn xuất, sinh lại bằng `npm run abi`.
contracts/build/

# contracts/deployments/ thì CÓ commit: nó là bản ghi địa chỉ đã triển khai và tx hash,
# không phải file dẫn xuất. Mất nó là mất dấu vết buổi deploy.

# === Sinh tự động ===
app/src/api/generated/
backend/src/main/java/**/contracts/generated/

# === IDE / OS ===
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# === Dữ liệu cục bộ ===
uploads/
mysql-data/
